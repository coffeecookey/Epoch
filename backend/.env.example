# CosyLab API Configuration
# Add your API key provided by cosylab.iiitd.edu.in
COSYLAB_API_KEY=your_api_key_here

# RecipeDB org API (e.g. recipe2-api on port 6969): use Bearer auth and set base URL + endpoint
# RECIPEDB_BASE_URL=http://cosylab.iiitd.edu.in:6969/recipe2-api/recipe
# RECIPEDB_USE_BEARER_AUTH=true
# RECIPEDB_ORG_ENDPOINT=recipesinfo
# If the list endpoint is named "recipes" instead of "recipesinfo", set RECIPEDB_ORG_ENDPOINT=recipes
# If the org API is slow or far away, set RECIPEDB_TIMEOUT=25 (seconds)
# If the primary URL is unreachable (e.g. port 6969 blocked), set a fallback: RECIPEDB_FALLBACK_BASE_URL=https://...
# Rate limiting (avoid IP blocking): RECIPEDB_RATE_LIMIT_DELAY=0.5 (seconds between requests, 0=off)
# Limit search scope: RECIPEDB_MAX_SEARCH_PAGES=5 (max pages when searching by name; 1 page = 200 recipes)

# Optional: Enable LLM features (requires Gemini API key)
USE_LLM_AGENT=false
GEMINI_API_KEY=

# Optional: Other configuration
USE_LLM_EXPLANATIONS=false
